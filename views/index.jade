script(type='text/javascript')
  $(function() {
    var socket = io.connect();

    socket.on('access', function(data) {
      console.log(data);
      if ($('.page').size() > 30) {
        $('.page:last').remove();
      }
      var user = data.user;
      var date = new Date(data.timestamp);
      $('#loading').hide();
      $('#pages').prepend(
        '<p class="page">' +
          '<a href="/' + user.name + '">' +
            '<img src="' + user.profile_url + '" style="float: left;"/>' + 
          '</a>' +
          '<span style="font-weight: bold">' + data.title + '</span> ' +
          '(<a href="' + data.url + '">' + data.url + '</a>)' +
          '<br /><span style="color: #AEAEAE;">' + date.toLocaleString() + '</span>' + 
        '</p>');
    });
  });
h3 If you want to *stalk* who you follow on Twitter, 
  a(href="/signin")
    img(src="http://si0.twimg.com/images/dev/buttons/sign-in-with-twitter-l.png")
  .

h2 People are accessing these pages NOW.
#pages
  img#loading(src="/loading.gif")
